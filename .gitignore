# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# Build artefacts of the pipeline (regenerated by `make pipeline`)
db/consultbae.db
db/consultbae.db-journal
reports/*.csv
reports/*.json

# Audio uploads (regenerated by using the app; keep the folder itself)
app/uploads/*
!app/uploads/.gitkeep

# Local secrets
.env

# OS / editor noise
.DS_Store
.idea/
.vscode/
